# NexusFlow

NexusFlow coordinates workflows, triggers, events, approvals and multi-step operations.

CortexFlow reasons. NexusFlow orchestrates. BlackNode governs.

Current implementation is mapped to:

- `app.ares`
- `app.services.autonomy_service`
- `app.models.approval_record`

