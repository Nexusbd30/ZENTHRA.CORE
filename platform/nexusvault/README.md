# NexusVault

NexusVault owns enterprise knowledge, memory and RAG.

Every answer grounded in private knowledge must include verifiable sources.

Current implementation is mapped to:

- `app.intelligence`
- `app.db.vector`
- `app.models.knowledge_document`

