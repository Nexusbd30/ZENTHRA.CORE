# NexusAPI

NexusAPI owns integrations, tool registry and external system access.

Tools must be risk-classified, permission-checked and audited.

Current implementation is mapped to:

- `app.actions`
- `app.identity`
- `app.secops`
- `app.ingestion`

