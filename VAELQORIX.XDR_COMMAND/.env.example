# VAELQORIX FRONTEND (Vite) â€“ ejemplo producciÃ³n
#
# Copia este archivo a `.env` y completa los valores reales antes de construir.

# URL pÃºblica del backend (FastAPI detrÃ¡s de reverse proxy o servicio docker)
VITE_API_URL=http://127.0.0.1:8010
VITE_API_TIMEOUT_MS=45000

# Opcional solo para desarrollo local controlado.
# En produccion dejar vacio: /monitoring/* debe usar JWT admin o un proxy servidor.
VITE_VAELQORIX_MONITOR_TOKEN=

# Datos opcionales para visualizaciones (no funcionales si se dejan vacÃ­os)
VITE_VAELQORIX_LAN_CIDR=
VITE_VAELQORIX_VPN_CIDR=
VITE_VAELQORIX_PUBLIC_IP=
VITE_VAELQORIX_LAN_KEY_HOSTS=
VITE_VAELQORIX_EXTERNAL_MONITORED_HOSTS=

# URL pÃºblica de Prometheus si se necesita reescritura de links
VITE_PROMETHEUS_PUBLIC_URL=
